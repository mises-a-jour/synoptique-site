---
layout: base.njk
title: Philosophie
description: "Les titres en philosophie aux Éditions Synoptique et aux Presses Philosophiques."
permalink: /philosophie/
---
{% from "macros.njk" import bookcard %}
{% set titres = collections.livres | parGenre("philosophie") %}

<div class="page-lede wrap">
  <div class="label">Genre</div>
  <h1>Philosophie</h1>
  <p class="hero__lede">{{ pages.genres.philosophie_intro }}</p>
</div>

<section class="wrap">
  <div class="section-head">
    <h2>{{ titres.length }} titre{{ "s" if titres.length > 1 }}</h2>
    <a class="more" href="/catalogue/">Tout le catalogue →</a>
  </div>
  {% if titres.length > 0 %}
    <div class="book-grid">
      {% for item in titres %}{{ bookcard(item) }}{% endfor %}
    </div>
  {% else %}
    <p class="prose">Aucun titre dans cette catégorie pour le moment.</p>
  {% endif %}
</section>
