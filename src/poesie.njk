---
layout: base.njk
title: Poésie
description: "Les titres en poesie aux Éditions Synoptique et aux Presses Philosophiques."
permalink: /poesie/
---
{% from "macros.njk" import bookcard %}
{% set titres = collections.livres | parGenre("poesie") %}

<div class="page-lede wrap">
  <div class="label">Genre</div>
  <h1>Poésie</h1>
  <p class="hero__lede">La voix poétique aux Éditions Synoptique.</p>
</div>

<section class="wrap">
  <div class="section-head">
    <h2>{{ titres.length }} titre{{ "s" if titres.length > 1 }}</h2>
    <a class="more" href="/catalogue/">Tout le catalogue →</a>
  </div>
  {% if titres.length > 0 %}
    <div class="book-grid">
      {% for item in titres %}{{ bookcard(item) }}{% endfor %}
    </div>
  {% else %}
    <p class="prose">Aucun titre dans cette catégorie pour le moment.</p>
  {% endif %}
</section>
